{
  "name": "tokopay-bulk-withdrawal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bulk transaction gateway in front of the TOKOPAY API. Accepts arrays of transactions, persists them to a local embedded PGlite database, returns a batchId, and processes each entry in parallel via an in-process worker. Clients poll the batchId for per-transaction status, hash, and the full TOKOPAY response.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
